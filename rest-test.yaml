api_address: http://localhost:4200/
verbose: true

tests:
  - it: should return with STATUS_OK when sending a GET request to /health/
    route: health
    method: GET
    status: 200
  - route: health
    method: POST
    status: 405